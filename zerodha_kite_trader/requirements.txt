# --- Core ---
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Broker / Market data ---
kiteconnect>=4.2.0

# --- Data / Numerics ---
numpy>=1.26.0
pandas>=2.1.0

# Technical analysis. TA-Lib needs the C library installed on the host
# (see docs/INSTALLATION.md). The code falls back to a pure-pandas
# implementation when TA-Lib is unavailable, so this is optional.
TA-Lib>=0.4.28 ; platform_system != "Windows"

# --- Machine learning ---
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0
# TensorFlow is heavy and optional; the ML layer degrades gracefully without it.
# tensorflow>=2.15.0

# --- Persistence ---
SQLAlchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.0
redis>=5.0.0

# --- API / Dashboard ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
websockets>=12.0

# --- Notifications ---
httpx>=0.26.0

# --- Misc ---
tenacity>=8.2.0
pytz>=2024.1
